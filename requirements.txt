# --- Web server / UI ---
# IMPORTANT: gradio>=5 is required. Gradio 4.x imports `HfFolder` from
# huggingface_hub, which was removed in huggingface_hub 1.x -> ImportError.
gradio>=5
fastapi>=0.110
uvicorn>=0.29
python-dotenv>=1.0

# --- Speech recognition ---
faster-whisper>=1.0.0

# --- Retrieval (RAG) ---
sentence-transformers>=2.6
pandas>=2.0
numpy>=1.24

# --- Translation ---
deep-translator>=1.11

# --- Text to speech ---
gTTS>=2.5

# --- HTTP (Ollama / OSRM / Nominatim) ---
requests>=2.31
